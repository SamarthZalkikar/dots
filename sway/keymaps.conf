##############################
#      set variables
##############################
set $mod Mod4
set $terminal foot
set $pdfreader zathura
set $blueman blueman-manager
set $notify swaync-client -t
set $powermenu ~/.config/custom_scripts/power.sh
set $lockscreen swaylock -i /home/samarth/Pictures/wallpapers/Graphite_Mono/wallhaven-w55x2q.png
bindsym $mod+V exec cliphist list | rofi -dmenu | cliphist decode | wl-copy


##############################
#     Floating Modifier      #
##############################
floating_modifier $mod

##############################
#  Basic operations  #
##############################
#bindsym $mod+Tab workspace next
#bindsym Mod1+Tab workspace prev
bindsym $mod+Shift+q kill
bindsym $mod+Shift+r reload

##############################
# switch to workspace        #
##############################
bindsym $mod+1    workspace  1
bindsym $mod+2    workspace  2
bindsym $mod+3    workspace  3
bindsym $mod+4    workspace  4
bindsym $mod+5    workspace  5
bindsym $mod+6    workspace  6
bindsym $mod+7    workspace  7
bindsym $mod+8    workspace  8
bindsym $mod+9    workspace  9
bindsym $mod+0    workspace  10

# move focused container to workspace
bindsym $mod+Shift+1    move container to workspace  1
bindsym $mod+Shift+2    move container to workspace  2
bindsym $mod+Shift+3    move container to workspace  3
bindsym $mod+Shift+4    move container to workspace  4
bindsym $mod+Shift+5    move container to workspace  5
bindsym $mod+Shift+6    move container to workspace  6
bindsym $mod+Shift+7    move container to workspace  7
bindsym $mod+Shift+8    move container to workspace  8
bindsym $mod+Shift+9    move container to workspace  9
bindsym $mod+Shift+0    move container to workspace  10

##############################
# Focus Changing
##############################

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

##############################
# Window Modifying Rules
##############################

# split in horizontal orientation
bindsym $mod+z split h

# split in vertical orientation
bindsym $mod+a split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+s layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle ; [floating] resize set 800 600 ; move position center

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+u focus parent
bindsym $mod+i focus child

##############################
#  Window Resizing           #
##############################
# resize window (you can also use the mouse for that):
bindsym $mod+Ctrl+h resize grow width 5 px or 5 ppt
bindsym $mod+Ctrl+j resize shrink height 5 px or 5 ppt
bindsym $mod+Ctrl+k resize grow height 5 px or 5 ppt
bindsym $mod+Ctrl+l resize shrink width 5 px or 5 ppt

####################################################
# keybindings for different shortcuts and actions: #
###################################################

## App shortcuts

# Browser
bindsym $mod+w exec brave

# File Manager
# bindsym $mod+n exec $filemanager

# PDF Reader Zathura 
bindsym $mod+Shift+o exec $pdfreader

# start a terminal
bindsym $mod+Return exec $terminal

# bluetooth manager
bindsym $mod+Shift+b exec $blueman

# swaync
# bindsym $mod+Shift+n exec $notify

# exit-menu
bindsym $mod+Shift+backspace exec $powermenu

# Lock the system
bindsym $mod+Shift+x exec $lockscreen

# Rofi
#bindsym $mod+d exec --no-startup-id rofi -show drun -disable-history -show-icons
bindsym $mod+d exec --no-startup-id rofi -show drun
bindsym Mod1+d exec --no-startup-id rofi -show run -disable-history
bindsym Mod1+w exec rofi -show window -show-icons
bindsym $mod+period exec rofi -modi emoji -show emoji
bindsym Mod1+Tab exec rofi -show window

# Screenshot : Screenshot
#bindsym Print exec grim -g "$(slurp)" /home/samarth/Pictures/Screenshots/Screenshots_$(date + '%Y-%m-%d_%H-%M-%S').png 
bindsym Print exec grim -g "$(slurp)" /home/samarth/Pictures/Screenshots/Screenshots_$(date +\%Y-\%m-\%d_\%H-\%M-\%S).png

##############################
# Funtions Keys
##############################
# Brightness
bindsym XF86MonBrightnessUp exec brightnessctl set +5%
bindsym XF86MonBrightnessDown exec brightnessctl set 5%-

# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec pactl set-sink-volume 0 +5% 
bindsym XF86AudioLowerVolume exec pactl set-sink-volume 0 -5% 
bindsym XF86AudioMute exec pactl set-sink-mute 0 toggle

# Mic mute
bindsym XF86AudioMicMute exec pamixer --default-source -t

# Media player controls
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioPause exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# Wallpaper
set $wal ~/.config/custom_scripts/chooseBackgroundImage.sh
bindsym $mod+Shift+w exec $wal

# Emacs
bindsym $mod+e exec emacs
